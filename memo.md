# internship

## memo

### 05/19
#### 015_multi_communication.rab
- 証明のために::Sent() (送信完了)をsend (送信)よりも先に記述することが実際の実装に与える影響はないのか

#### 全体
- 思っていたよりもrabbitのコードに依存する面が大きい（実装とrabbitコードの間に差がないことの証明はできない？）
- 何を検証するかもrabbitコード依存
- 権限管理のときはchannelでどうにかする？

### 05/26
TCP通信を再現できない？
- かなり簡略化した(簡略化しすぎな)TCP 3-way handshake
- シーケンス番号はデータ転送により本来書き換わる
- 確認応答番号も使っていない
    + 確認応答番号を使って信頼できない(＝途中でパケットロスが起きるかもしれない)通信路での通信を再現できないか
- 検証すべきものが何かわからない
    + 機密性・完全性（・可用性）？
    + 真正性・信頼性・責任追求性・否認防止性

### 06/02
- コネクションが成立したらプロセスごとにparamで隔離する方が実際に近そう(TODO)
- パケットロスを再現できないか
    + 確率で落ちることの再現は難しそう？
    + 誤った順序でパケットが受け入れられることをfalsifiedする？
- このまま拡張していくことはできそうだが...
- ネットワーク検証というよりはプロトコル検証...
- 途中途中に色々挟めばだいぶ検証しやすい

### 06/09
- かかった時間は41秒くらい（04_simple_tcp_tls）
- 05_simple_ca の場合は140秒くらい
- 攻撃者に公開鍵の情報を教えるために pk_s を送ったが、実際には冗長
- 秘密鍵の漏洩前後で情報の漏洩がどう変わるかを試したいが、かなり時間がかかりそう...
- 秘密が漏れてしまっているとしたら、この時点で秘密鍵が漏れているせいだということを証明する
- Reachableに使うものとSecretに使うものが（頭の中では）違っているので検証時にそれを分けられると良いのでは

### 06/16
- builtins の Diffie-Hellman を使ってみる（Tamarinを書き換える）
- 昔の Diffie-Hellman を今の Tamarin で使ってみる
- https://github.com/tamarin-prover/tamarin-prover/blob/master/examples/experiments/A-GDH2.spthy

### 06/23
- CAによる署名も加えたい
- g^(xy)が漏れていたわけではないからverifiedされてしまっていた
    + 切り分けミスと簡略化のしすぎ

" 'g'^x "とか


## 覚えておくこと
``` eval opam env ```
グラフ化可能

